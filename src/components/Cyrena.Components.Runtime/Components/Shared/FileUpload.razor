@inherits KernelComponentBase

@if (!string.IsNullOrEmpty(_accepts))
{
    <MudTooltip Text="Attach File">
        <MudFab StartIcon="@Icons.Material.Filled.AttachFile" Size="Size.Small" Color="Color.Secondary" Class="rounded-pill" OnClick="TriggerFileUpload" />
    </MudTooltip>

   
    <!-- Hidden File Input -->
    <InputFile @ref="_fileInput"
               OnChange="HandleFilesSelected"
               multiple
               accept="@_accepts"
               style="display: none;" />
}
