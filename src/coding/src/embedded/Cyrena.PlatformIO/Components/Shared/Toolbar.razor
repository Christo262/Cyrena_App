@inherits KernelComponentBase

<MudSelect T="string" Value="@_name" ValueChanged="OnChange" Disabled="_its.Inferring" Style="max-width:250px">
    @foreach (var item in _environment.Environments)
    {
        <MudSelectItem Value="@item.Name">@item.Name</MudSelectItem>
    }
    </MudSelect>