@using Cyrena.Components.Shared

<MudDialog>
    <DialogContent>
        <MudGrid>
            @foreach (var env in _environments)
            {
                <MudItem xs="12" sm="6" md="4">
                    <MudCard @onclick="() => SelectEnvironment(env)" Class="cursor-pointer">
                        <MudCardContent>
                            <MudText Typo="Typo.h6">@env</MudText>
                        </MudCardContent>
                    </MudCard>
                </MudItem>
            }
        </MudGrid>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel" Color="Color.Error">Cancel</MudButton>
    </DialogActions>
</MudDialog>