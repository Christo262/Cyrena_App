@page "/converse/{Id}"
@using Cyrena.Components.Shared
@attribute [TabItemOption(Text = "Converse")]

@if (_kernel != null)
{
    <div class="row m-0 p-0 h-100">
        <div class="col overflow-auto p-0 h-100">
            <Chat Kernel="_kernel" />
        </div>
        <Docking Kernel="_kernel" />
    </div>
}